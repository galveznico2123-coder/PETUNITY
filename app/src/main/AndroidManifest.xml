<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.petunity"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".PetUnityApplication"
        android:allowBackup="true"
        android:icon="@drawable/petunity_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/petunity_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetUnity"
        tools:targetApi="31">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PetUnity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.VerificationActivity"
            android:exported="false"
            android:label="Verify Your Email" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.RescuerMainActivity"
            android:exported="false"
            android:label="PetUnity Rescue" />

        <activity
            android:name=".activities.MyPostsActivity"
            android:exported="false"
            android:label="My Reports" />

        <activity
            android:name=".activities.MyPetsActivity"
            android:exported="false"
            android:label="My Registered Pets" />

        <activity
            android:name=".activities.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.AddPetActivity"
            android:exported="false"
            android:label="Pet Registration"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.EditProfileActivity"
            android:exported="false"
            android:label="Edit Profile"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Cloudinary Service Required for Background Uploads -->
        <service
            android:name="com.cloudinary.android.UploadService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>